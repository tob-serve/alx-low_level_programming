#include "main.h"

/**
  * _abs - Computes the absolute value
  * of an integer.
  *
  * @int: input number as an integer
  *
  * Return: absolute value
  */

int _abs(int d)
{
	if (d >= 0)
	{
		return (1);
	}
	else
	{
		return (1 * -1);
	}
}
